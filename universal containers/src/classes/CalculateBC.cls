public Interface CalculateBC {
	
	 Integer calculateHRA(Position__c p);
	 Double  calculatePremium(Position__c p);
     Double  calculatePremium2(Position__c p);
      Double  calculatePremium4(Position__c p);
}